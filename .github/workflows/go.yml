name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.27'

    - name: install
      run: |
        go install -v golang.org/x/tools/cmd/goimports@latest
        go install -v github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2

    - name: Test
      run: |
        make fmt && git diff --exit-code
        make imports && git diff --exit-code
        make lint-new
        make test

    - name: govulncheck
      continue-on-error: true
      run: |
        go install golang.org/x/vuln/cmd/govulncheck@latest
        govulncheck ./...

  # The upstream promql fixture sweep runs ~16m, so it gets its own job rather
  # than slowing every `make test`.
  upstream-promql:
    name: Upstream PromQL fixtures
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.27'

    - name: Test
      run: make test-upstream
